// const chai = require('chai')
// const expect = chai.expect;
//
// describe('This is to make sure we are using the testing env', () => {
//     it('makes sure we are running in a test env', () => {
//         expect({env: process.env.DB_ENV}).to.include('testing')
//     })
// })
